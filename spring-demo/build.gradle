plugins {
    id 'groovy'
    id 'java'
}

group 'com.spring.demo'
version '1.0.0'

repositories {
    mavenCentral()
}

dependencies {
    compile(project(":spring-context"))
    testCompile group: 'junit', name: 'junit', version: '4.12'
}

test {
    useJUnitPlatform()
}